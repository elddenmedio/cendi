import { TotalsLoopPipe } from './totals-loop.pipe';

describe('TotalsLoopPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalsLoopPipe();
    expect(pipe).toBeTruthy();
  });
});
