import { TotalsPercentPipe } from './totals-percent.pipe';

describe('TotalsPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalsPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
