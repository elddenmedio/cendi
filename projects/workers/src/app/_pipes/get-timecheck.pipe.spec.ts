import { GetTimecheckPipe } from './get-timecheck.pipe';

describe('GetTimecheckPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTimecheckPipe();
    expect(pipe).toBeTruthy();
  });
});
